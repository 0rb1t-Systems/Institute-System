/**
 * Re-export the single toast store used by <Toaster />.
 * All app code should toast through @/components/ui/use-toast (or @/lib/notify).
 */
export { useToast, toast } from '@/components/ui/use-toast';
